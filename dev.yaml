version: '3'
services:
  postgres:
    container_name: postgres_dev
    build:
      context: ./server/postgres
    image:  ${FULL_IMAGE_NAME}/postgres_dev:v2
    ports:
      - "5432:5432"
    # TODO - replace this by creating a default superadmin with SQL for phase 4
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
  api:
    container_name: api_dev
    working_dir: /usr/cloud-apps/api
    environment:
      - PGUSER=postgres
      - PGHOST=postgres
      - PGDATABASE=postgres
      - PGPASSWORD=postgres_password
      - PGPORT=5432
      - WAIT_HOSTS=postgres:5432
    ports:
      - "4000:4000"
    build:
      context: ./server/api
      dockerfile: docker/Dockerfile.dev
    image:  ${FULL_IMAGE_NAME}/api_dev:v2
    volumes:
      - ./server/api:/usr/cloud-apps/api
      - /usr/cloud-apps/api/node_modules
    command: |
      sh -c "/wait && npm run dev"
  ui:
    container_name: ui_dev
    working_dir: /usr/cloud-apps/ui
    environment:
      - WAIT_HOSTS=api:4000
    ports:
      - "3000:3000"
    build:
      context: ./client/ui
      dockerfile: docker/Dockerfile.dev
    image:  ${FULL_IMAGE_NAME}/ui_dev:v2  
    volumes:
      - ./client/ui:/usr/cloud-apps/ui
      - /usr/cloud-apps/ui/node_modules
    command: |
      sh -c "/wait && npm run dev"
