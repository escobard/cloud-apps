describe("useGetRequest hook", () => {
  it(">> updates state with request response", () => {});
  it(">> does not update state with request response if unmounted", () => {});
  it(">> returns error in case of request rejections", () => {});
});