{
  "name": "integration",
  "version": "0.0.2",
  "description": "integration",
  "main": "config.test.js",
  "scripts": {
    "start": "npm run test",
    "test": "mocha ./{,!(node_modules)/**/}*.test.js",
    "test-watch": "set PORT=5000 && mocha --watch ./{,!(node_modules)/**/}*.test.js",
    "test-in-order": "mocha --bail ./tests/tests-in-order.js"
  },
  "engines": {
    "node": "10.x",
    "npm": "6.x"
  },
  "author": "escobard",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "supertest": "^3.4.2"
  }
}
