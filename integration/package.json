{
  "name": "integration",
  "version": "0.0.2",
  "description": "integration",
  "main": "config.test.js",
  "scripts": {
    "test": "nyc --reporter=text mocha ./{,!(node_modules)/**/}*.test.js",
    "test-watch": "set PORT=5555 && nyc --reporter=text mocha --watch ./{,!(node_modules)/**/}*.test.js"
  },
  "engines": {
    "node": "10.x",
    "npm": "6.x"
  },
  "author": "escobard",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "nyc": "^14.1.1",
    "supertest": "^3.4.2"
  }
}
